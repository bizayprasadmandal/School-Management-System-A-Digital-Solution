# EduSphere Web — Environment Configuration
REACT_APP_API_URL=http://localhost:8000/api/v1
REACT_APP_WS_URL=ws://localhost:8000

# Sentry error tracking (optional — leave unset to disable Sentry entirely)
# Get a DSN from your Sentry project: Settings → Projects → <project> → Client Keys (DSN)
REACT_APP_SENTRY_DSN=
# Optional release tag (e.g. a git SHA). Defaults to nothing when unset.
REACT_APP_SENTRY_RELEASE=
